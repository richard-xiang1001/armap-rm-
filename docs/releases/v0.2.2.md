# v0.2.2 — Documentation/Release Alignment Patch

Release date: 2026-02-16

## Scope

- No model, data schema, or training-pipeline behavior changes.
- This patch aligns release artifacts with the already-passed v0.2.1 acceptance run.

## Why v0.2.2 Exists

- Tag `v0.2.1` was created before the final reference-run documentation commit.
- `v0.2.2` ensures users pulling the release tag see the complete acceptance evidence and interpretation notes.

## Included Updates

- README top-level acceptance-closure summary:
  - real trajectory ingest + strict gate
  - 4090 bf16 training acceptance
  - CPU/GPU parity check
- Documentation records for v0.2.1 reference run and metric interpretation (`gap_p50` near zero but positive).

## Validation Statement (Inherited from v0.2.1 Acceptance)

- data: `pairs_out=5500`, `train/valid=5000/500`
- strict gate: `missing=0`, `leak=0`, `truncation_risk_last_k_steps=0.0`
- training: `pair_accuracy=0.618`, `gap_p50=0.005510792136192322`
- parity: CPU/GPU pair-accuracy diff `=0.0`
- acceptance report: `data/real/reports/v021_acceptance.json` (`passed=true`)
