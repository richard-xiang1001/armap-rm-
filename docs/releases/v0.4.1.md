# v0.4.1 — Judge Protocol and Cross-Env Migration

Release date: 2026-02-16

## Scope

- Move env judge to a protocol-driven interface.
- Support judge registry and runtime selection by CLI.
- Keep v0.3 acceptance path backward compatible via default `arithmetic` judge.

## Added

- Judge protocol and compare API:
  - `/Users/xiangruichao/Desktop/armap-rm-repro/ingest/envs/protocol.py`
- Judge registry:
  - `/Users/xiangruichao/Desktop/armap-rm-repro/ingest/envs/registry.py`
- Real logs judge:
  - `/Users/xiangruichao/Desktop/armap-rm-repro/ingest/envs/real_logs_env.py`

## Changed

- Pair construction now records:
  - `judge_unknown`
  - `judge_unknown_reason`
  - `judge_protocol_version`
- Negative generators now consume injected env judge instead of hard-coded arithmetic judge.

## Validation

- `scripts/run_v030_acceptance.py` supports `--env_judge` and unknown-rate gates.
- `scripts/run_v040_real_acceptance.py` runs with `--env_judge real_logs`.

## Protocol Migration Evidence (2026-02-16)

Two acceptance runs were executed on the same pipeline with different judges.

### 1) Arithmetic judge run

- command baseline:
  - `python3 scripts/run_v030_acceptance.py --raw_path data/raw/example_episodes_v030.jsonl --adapter generic_jsonl --env_judge arithmetic --work_dir data/v041_arithmetic --results_dir results/v041_arithmetic --max_pairs 200 --target_train_size 160 --target_valid_size 40 --min_replay_ok_rate 0.0 --min_env_judge_consistency 0.95 --max_judge_unknown_rate 0.05 --min_pair_accuracy 0.0 --min_gap_p50 -1.0`
- report:
  - `/Users/xiangruichao/Desktop/armap-rm-repro/data/v041_arithmetic/reports/v030_acceptance.json`
- result:
  - `passed=true`
  - `judge_unknown_rate=0.0`
  - `env_judge_consistency=1.0`

### 2) Real-logs judge run

- command baseline:
  - `python3 scripts/run_v030_acceptance.py --raw_path data/raw/episodes_real_v040.jsonl --adapter real_logs_v1 --env_judge real_logs --work_dir data/v041_real_logs --results_dir results/v041_real_logs --max_pairs 31 --target_train_size 20 --target_valid_size 11 --min_replay_ok_rate 0.0 --min_env_judge_consistency 0.95 --max_judge_unknown_rate 0.05 --min_pair_accuracy 0.0 --min_gap_p50 -1.0`
- report:
  - `/Users/xiangruichao/Desktop/armap-rm-repro/data/v041_real_logs/reports/v030_acceptance.json`
- result:
  - `passed=true`
  - `judge_unknown_rate=0.0`
  - `env_judge_consistency=1.0`

Interpretation:
- Judge protocol migration is verified: arithmetic and real_logs judges both run through the same construction/gate/train/eval pipeline with complete reports and deterministic parity (`pair_accuracy_parity_diff=0.0` in both runs), and both satisfy strict unknown/consistency gates in this migration proof run.
