# v0.2.1 — Acceptance Round (5000/500 + CPU/GPU Parity)

Release date: 2026-02-16

## Highlights

- Added one-command acceptance runner:
  - `scripts/run_v021_acceptance.py`
  - covers export/sanitize/lint gate/train/eval/parity checks
- Added exact validation-size split support in pipeline:
  - `scripts/run_v020_pipeline.py --valid_size`

## Acceptance Targets

- Data size target:
  - `train=5000`, `valid=500`
- Gate target:
  - `rows_with_missing=0`
  - `leak_hit_count=0`
  - `truncation_risk_last_k_steps<=0.05`
- Training target:
  - `pair_accuracy>0.55`
  - `gap_p50>0`
- Parity target:
  - `abs(pair_acc_gpu - pair_acc_cpu)<=0.03`

## Canonical Command

```bash
python3 scripts/run_v021_acceptance.py \
  --raw_path data/raw/episodes_real.jsonl \
  --adapter generic_jsonl \
  --work_dir data/real \
  --results_dir results/v021_real \
  --max_pairs 5500 \
  --target_train_size 5000 \
  --target_valid_size 500 \
  --max_len 512 \
  --batch_size 256 \
  --epochs 3 \
  --max_steps 4000 \
  --device cuda \
  --amp_dtype bf16 \
  --num_workers 4
```

Output report:
- `data/real/reports/v021_acceptance.json`

## Reference Run (AutoDL 4090)

Run date: 2026-02-16

Environment:
- GPU: `NVIDIA GeForce RTX 4090` (24GB)
- Driver: `560.35.03`
- System CUDA: `12.6`
- Runtime env: `conda env = armap-rm`

Input/data summary:
- Raw episodes: `data/raw/episodes_real.jsonl`
- `episodes_in=12000`
- `pairs_out=5500`
- strategy counts:
  - `same_task_failure=5500`
- split:
  - `train=5000`
  - `valid=500`

Gate summary (`data/real/reports/train_lint.json`):
- `rows_with_missing=0`
- `leak_hit_count=0`
- `truncation_risk_last_k_steps=0.0` (threshold `<=0.05`)

Training/eval summary (`results/v021_real`):
- best valid `pair_accuracy=0.618`
- final eval:
  - `pair_accuracy=0.618`
  - `avg_reward_gap=0.004093139052391052`
  - `gap_p10=-0.016657302156090736`
  - `gap_p50=0.005510792136192322`
  - `gap_p90=0.023761045187711716`
  - `n=500`

Metric interpretation note:
- This run has `gap_p50=0.005510792136192322` (positive but close to zero). In real-trajectory settings with harder negatives, this is expected.
- `pair_accuracy` remains the primary acceptance metric for preference ranking quality.
- `gap`/`gap_p50` is used as a directional health signal (`>0`), not as a monotonic “larger-is-always-better” KPI.

CPU/GPU parity (same checkpoint + same valid + `max_len=512` + `num_workers=0`):
- GPU eval `pair_accuracy=0.618`
- CPU eval `pair_accuracy=0.618`
- `abs(diff)=0.0` (target `<=0.03`)

Acceptance result:
- report: `data/real/reports/v021_acceptance.json`
- `passed=true`
