# v0.4.0 — Real Evidence MVP

Release date: 2026-02-16

## Scope

- Introduce real-logs adapter and acceptance pipeline for v0.4.0.
- Keep text-only RM training path lightweight and 4090-friendly.
- Enforce unknown-judge filtering before train/valid split.

## Added

- Real logs adapter:
  - `/Users/xiangruichao/Desktop/armap-rm-repro/ingest/adapters/real_logs_v1.py`
- v0.4.0 acceptance runner:
  - `/Users/xiangruichao/Desktop/armap-rm-repro/scripts/run_v040_real_acceptance.py`
- Optional sample generator for sanitized real-like logs:
  - `/Users/xiangruichao/Desktop/armap-rm-repro/scripts/generate_real_logs_v040_sample.py`

## Acceptance Command

```bash
python3 scripts/run_v040_real_acceptance.py \
  --raw_path data/raw/episodes_real_v040.jsonl \
  --adapter real_logs_v1 \
  --env_judge real_logs \
  --work_dir data/real_v040 \
  --results_dir results/v040_real \
  --max_pairs 1200 \
  --target_train_size 1000 \
  --target_valid_size 200
```

## Expected Artifacts

- `/Users/xiangruichao/Desktop/armap-rm-repro/data/real_v040/reports/v040_real_acceptance.json`
- `/Users/xiangruichao/Desktop/armap-rm-repro/results/v040_real/metrics.jsonl`
- `/Users/xiangruichao/Desktop/armap-rm-repro/data/real_v040/reports/train_lint.json`
- `/Users/xiangruichao/Desktop/armap-rm-repro/data/real_v040/reports/sanitize_audit.json`

## Notes

- `judge_unknown` samples are filtered out before sanitize/lint/train.
- `judge_unknown_rate` is part of lint and acceptance gates.

## Reference Run Evidence (2026-02-16)

### A) Release-grade run status (blocked)

Executed command (release target):

```bash
python3 scripts/run_v040_real_acceptance.py \
  --raw_path data/raw/episodes_real_v040.jsonl \
  --adapter real_logs_v1 \
  --env_judge real_logs \
  --work_dir data/real_v040 \
  --results_dir results/v040_real \
  --max_pairs 1200 \
  --target_train_size 1000 \
  --target_valid_size 200
```

Observed blocker:
- dataset size insufficient: `episodes_in=60`, `pairs_trainable=31`
- quality gates are healthy on current sample:
  - `judge_unknown_rate=0.0`
  - `env_judge_consistency=1.0`
  - `pair_accuracy(valid)=1.0`, `gap_p50=0.004005271941423416`
  - `pair_accuracy_parity_diff=0.0`

Artifacts produced during blocked run:
- `/Users/xiangruichao/Desktop/armap-rm-repro/data/real_v040/reports/export_stats_v040.json`
- `/Users/xiangruichao/Desktop/armap-rm-repro/data/real_v040/reports/train_lint.json`
- `/Users/xiangruichao/Desktop/armap-rm-repro/data/real_v040/reports/sanitize_audit.json`
- `/Users/xiangruichao/Desktop/armap-rm-repro/data/real_v040/reports/v040_real_acceptance.json` (`passed=false`, scale-only blocker)
- `/Users/xiangruichao/Desktop/armap-rm-repro/data/real_v040/reports/precheck_pairs_summary.json`

Conclusion:
- v0.4.0 release-grade evidence is blocked only by data scale (`pairs_trainable < 1200`), not by judge/lint/train quality gates.

### B) Non-release precheck run (for pipeline verification)

Executed precheck command:

```bash
python3 scripts/run_v040_real_acceptance.py \
  --raw_path data/raw/episodes_real_v040.jsonl \
  --adapter real_logs_v1 \
  --env_judge real_logs \
  --work_dir data/real_v040_precheck \
  --results_dir results/v040_real_precheck \
  --max_pairs 31 \
  --target_train_size 20 \
  --target_valid_size 11 \
  --min_replay_ok_rate 0.0 \
  --min_env_judge_consistency 0.0 \
  --max_judge_unknown_rate 1.0 \
  --min_pair_accuracy 0.0 \
  --min_gap_p50 -1.0
```

Precheck outputs:
- `/Users/xiangruichao/Desktop/armap-rm-repro/data/real_v040_precheck/reports/v040_real_acceptance.json` (`passed=true`)
- `/Users/xiangruichao/Desktop/armap-rm-repro/results/v040_real_precheck/metrics.jsonl`
- `/Users/xiangruichao/Desktop/armap-rm-repro/data/real_v040/reports/precheck_pairs_summary.json` (`LT_300_DATA_SCALE_BLOCKER`)

Key precheck metrics:
- `pair_accuracy=1.0`
- `gap_p50=0.003827570006251335`
- `judge_unknown_rate=0.0`
- `pair_accuracy_parity_diff=0.0`

Release decision:
- Keep `v0.4.0` tag pending until a full real-log run reaches `max_pairs=1200` with `target_train_size=1000` and `target_valid_size=200`.
